# Optional: simple login for /statement and /admin (values are embedded in the client bundle at build time).
# Vercel: Project → Settings → Environment Variables — add both, then redeploy.
VITE_APP_LOGIN_USERNAME=
VITE_APP_LOGIN_PASSWORD=
